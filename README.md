# Final C# API Project

## Status
- Project builds and runs successfully (`dotnet build && dotnet run`).
- Hosted locally at `http://localhost:5235`.
- Encountered 404 errors when testing `/api/hello`.

## Attempts and Progress
- Built and ran project multiple times with successful compilation.
- Added both `HelloController.cs` and `HelloWorldController.cs` to troubleshoot routing.
- Followed step-by-step guidance to configure `Program.cs` and controllers.
- Pushed project to GitHub for review.

## Notes
This submission includes all source code and logs of attempts. The project builds and runs, but endpoint routing requires further debugging.
